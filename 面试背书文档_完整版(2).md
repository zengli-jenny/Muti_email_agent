# 曾理 - AI应用工程师面试背书文档（完整版）

> 本文档是面试前的完整准备材料，按项目组织，涵盖团队构成、技术全流程、训练细节、踩坑故事、面试高频问题与应对策略。请以「讲故事」的方式内化，不要死记硬背。

---

## 一、团队构成与分工

### 千岸科技AI团队（3人小组）

| 角色 | 职责 |
|------|------|
| **产品经理（1人）** | 负责需求调研、和客服/品质/市场部门对接业务规则、验收标准定义、上线后效果跟踪 |
| **我 - AI开发工程师（1人）** | 核心技术负责人，负责Agent架构设计、模型选型/微调/部署、RAG搭建、Prompt工程、工作流编排等所有AI相关开发 |
| **传统后端开发（1人）** | 负责ERP数据接口开发（订单/物流/标签等业务数据API）、打标结果回写ERP、准确率统计报表、定时任务调度等基础设施支持 |

**面试时怎么说：**
> "我们是一个3人的小团队，产品经理负责和业务部门对接需求，我负责所有AI技术侧的工作——从架构设计到模型训练到部署上线，还有一个后端同事负责给我提供业务数据接口和做一些统计报表。因为团队小，所以我基本上是全栈在做AI这块，从选型到微调到RAG到Agent架构都是我一个人搞的。"

---

## 二、项目一：跨境电商智能客服自动回复系统（重点项目）

### 2.1 项目全景故事

> "这个项目分了两个大阶段。第一阶段是25年3月到8月左右，我们把辅助生成模式做到了95%采纳率，但发现人效没有本质提升——客服还是要一封一封点开看、确认、发送。所以第二阶段从25年下半年开始，目标是做全自动回复，让AI直接代替人发邮件。"

> "一开始我其实是用阿里云百炼的低代码工作流搭的，速度很快，一两周就能跑通一个场景。但很快就碰到瓶颈了——百炼的工作流不支持动态子流程调用、不能做复杂的条件跳转、更别说什么反思纠错了。所以后来我把核心决策层重构成了LangGraph的Multi-Agent架构，但一些简单的辅助流程（比如FAQ自动回复、模板邮件生成）还是用百炼或Dify搭的，哪个快用哪个。"

### 2.2 Multi-Agent架构详解

**五个Agent的职责：**

1. **Router Agent（意图路由）**
   - 输入：客户邮件原文 + 历史对话
   - 做的事：识别邮件包含几个诉求、每个诉求的类型（退款/换货/物流/咨询/投诉等）
   - 实现：一个LLM调用，Prompt里定义了11个场景的识别规则，输出结构化JSON
   - 难点：单封邮件经常有2-3个诉求混在一起，比如"我接受退款，但另一个订单还没收到"

2. **Retriever Agent（知识检索）**
   - 拿到路由结果后，根据诉求类型决定检索策略
   - 产品参数类问题 → ES BM25精确匹配
   - 模糊咨询类 → Milvus向量检索
   - 产品兼容/配件关系 → Neo4j图谱查询
   - 最后用Cross-Encoder（bge-reranker-v2-m3）对三路结果统一重排序

3. **Solver Agent（方案决策）- 核心，ReAct实现**
   - 这是整个系统最复杂的Agent
   - 实现方式：LangGraph的StateGraph，每个节点是一个状态
   - ReAct循环：
     ```
     Thought: "客户说订单#ohuhu290027没收到，我需要先查物流状态"
     Action: call_tool("track_logistics", {"order_id": "#ohuhu290027"})
     Observation: "物流显示已签收，签收时间2025-06-15"
     Thought: "已签收但客户说没收到，需要查是否有签收证据（签名/照片）"
     Action: call_tool("get_delivery_proof", {"tracking_number": "xxx"})
     Observation: "有投递照片"
     Thought: "有签收证据，按照公司政策应该让客户核实..."
     Action: generate_reply(...)
     ```
   - 最多允许5轮循环，超过就降级人工

4. **Executor Agent（动作执行）**
   - 当方案确定后，执行具体操作：调退款API、创建换货单、发送邮件
   - 有些操作需要审批（比如金额>50美元的退款），会打标为"待人工确认"

5. **Reviewer Agent（质量审核）- Reflection实现**
   - 对生成的邮件做三项检查：
     - 事实一致性：邮件中提到的订单号、金额、物流状态是否和查询结果一致
     - 方案合规性：退款金额是否超出授权范围、换货商品是否有库存
     - 品牌调性：语气是否符合品牌要求（比如Ohuhu要亲和、Tribit要专业）
   - 如果发现问题，会生成反馈信息回传给Solver Agent重新生成
   - 实际运行中大概15%的邮件会触发一次Reflection

**面试官可能的追问：**

Q: "ReAct和Reflection具体怎么实现的？是自己写的还是用的框架？"
> "ReAct是基于LangGraph的StateGraph实现的。我定义了think、act、observe三个节点，通过条件边（conditional_edge）控制流转。当observe节点返回的结果需要进一步处理时，conditional_edge会把状态路由回think节点，形成循环。我设了一个max_iterations=5的限制，防止死循环。"
> 
> "Reflection是单独的一个Agent节点，不是在Solver内部做的。Reviewer Agent拿到最终邮件后做检查，如果发现问题，会更新State里的feedback字段，然后状态流转回Solver Agent。Solver Agent在Prompt里会看到feedback，知道上一次哪里出了问题，重新生成。一般一次Reflection就够了，极少需要两次。"

Q: "为什么用LangGraph而不是直接LangChain的AgentExecutor？"
> "AgentExecutor是单Agent的，适合简单场景。但我们的客服系统需要多个Agent协作——路由、检索、决策、执行、审核是不同的职责，各自有不同的Prompt和工具集。LangGraph的State Graph能让这些Agent共享状态但独立运行，而且支持条件分支和循环，比较适合这种多步骤的复杂工作流。"

### 2.3 Hybrid RAG细节

**ES BM25：**
- 索引了约2000条产品参数文档（每个SKU一条）和500+条售后政策条款
- 当客户问"IC-BK28键盘是不是蓝牙5.0"这种精确参数问题，向量检索效果不好（因为"蓝牙5.0"和"蓝牙5.3"向量距离很近），BM25精确匹配更靠谱

**Milvus向量检索：**
- Embedding模型：BGE-M3（支持中英多语言）
- 维度：1024
- 索引了所有产品FAQ、历史工单优秀回复、品牌话术模板，约1.5万条
- chunk策略：按语义段落切分，每个chunk 300-500 tokens，overlap 50 tokens
- 用的是LangChain的RecursiveCharacterTextSplitter，先按\n\n切，再按\n，最后按句号

**Neo4j知识图谱：**
- 节点类型：Product、SKU、Category、Accessory、Feature
- 关系类型：COMPATIBLE_WITH、REPLACES、BELONGS_TO、HAS_FEATURE
- 主要解决的场景：
  - 客户问"Ohuhu马克笔能用什么替换笔头" → 通过图谱遍历找到兼容配件
  - 客户买了A产品但想换B产品 → 图谱查询两者是否同系列、是否可以互换
  - 售前推荐："我想画水彩风格" → 图谱遍历"水彩风格"这个Feature关联的产品

**Cross-Encoder重排序：**
- 用的bge-reranker-v2-m3
- 三路召回结果合并去重后，统一用Cross-Encoder打分，取Top-5
- 为什么要重排序？因为三路结果的分数不可比（BM25分数和余弦相似度量级不同）

**面试官可能的追问：**

Q: "chunk怎么切的？有没有遇到切断关键信息的情况？"
> "一开始用的是固定长度512 tokens切分，确实会把一个完整的售后政策从中间截断。后来改成了语义切分——先按段落（双换行）切，如果段落太长再按句子切，设了overlap 50 tokens做缓冲。对于产品参数表格类的文档，我是按每个产品一条记录整条入库的，不做切分。"
> 
> "还有一个问题是多格式文档——我们的知识库来源有PDF产品手册、Excel参数表、Word售后政策文档。PDF用的PyMuPDF提取文本，Excel用pandas读取后按行转成文本，Word用python-docx。图文混排的PDF最麻烦，一些产品手册里的参数是图片里的表格，我用的PaddleOCR做了一遍OCR提取。"

Q: "图文召回怎么做的？"
> "严格来说我们没有做图文多模态召回。产品图片信息是通过OCR提取文本后和文字信息合并存储的。如果未来要做，我的思路是用CLIP或者BGE-M3的多模态版本把图片和文本统一到同一个向量空间，但当前业务场景还不需要。"

### 2.4 MCP工具集成

> "MCP协议是2025年底Anthropic提出的，我是26年初开始在系统里集成的。之前我们的工具调用是用LangChain的@tool装饰器写的Python函数，和Agent代码耦合在一起。MCP的好处是把工具定义标准化了——每个工具有自己的schema描述、输入输出定义、错误处理，和Agent代码解耦。"

> "我把5个业务API（订单查询、物流追踪、退款执行、换货处理、库存校验）按MCP规范封装成了一个MCP Server，Agent通过标准协议调用。这样后端同事更新API的时候不用动我的Agent代码，只要MCP Server的schema没变就行。"

### 2.5 Skills动态加载机制

> "Skills这个概念我是26年初在用Claude的时候接触到的，觉得这个思路特别好——把特定领域的知识和规则封装成独立的'技能文件'，模型在需要的时候去读取。"

> "我在我们的Agent系统里做了类似的实现：每个品牌的回复策略、促销活动规则、话术模板都是一个独立的Markdown文件（比如ohuhu_refund_policy.md、tribit_warranty_rules.md），存在一个skills目录下。Agent在处理某个品牌的邮件时，会先读取对应品牌的Skill文件注入到System Prompt里。"

> "一开始我是把所有品牌的规则都塞进一个大Prompt里，后来发现效果不好——Prompt太长了（超过8000 tokens），模型会注意力分散，经常忽略一些细则。改成Skills按需加载后，每次只注入当前品牌相关的2000-3000 tokens，效果明显好了。"

> "但也踩了一些坑。我发现Skills的效果和模型能力关系非常大——同样的Skill文件，用Claude处理的时候几乎100%遵循规则，但换成Qwen-Plus就经常漏掉一些条件判断。后来我专门针对Qwen的特点优化了Skill文件的写法：把关键规则加粗、用更明确的if-then格式、减少嵌套层级。所以我的一个体会是，Agent加了很多花哨的能力（ReAct、Reflection、Skills），但最终效果的天花板还是模型本身的能力决定的。"

**面试官可能的追问：**

Q: "Skills和RAG有什么区别？为什么不直接放到知识库里检索？"
> "Skills是确定性的——我明确知道处理Ohuhu品牌的退款就要读这个文件，不需要检索。RAG是不确定的——客户问了一个问题，我不知道答案在哪条文档里，需要检索。把策略规则放RAG里检索会有两个问题：一是可能检索不到，二是可能检索到别的品牌的规则造成混淆。Skills是精确加载，RAG是模糊检索，两者互补。"

### 2.6 长记忆管理

- 用Redis存储每个客户的关键事实（Key-Value结构，key是customer_email，value是事实列表）
- 事实类型：客户已提供的订单号、之前的投诉历史、已执行的解决方案、客户偏好语言
- 对话历史用滑动窗口压缩：保留最近3轮完整对话 + 之前对话的摘要（用LLM生成的100 tokens摘要）
- 为什么这么做：有些客户会跨多封邮件沟通同一个问题，如果没有长记忆，每封邮件都要重复问客户订单号

### 2.7 多语言适配

- 8个站点覆盖英/德/法/日/西/意/葡/荷等语言
- 不是用翻译API，而是直接用多语言Prompt模板
- 每个品牌×每个语言有一套Prompt模板，模板里包含当地客服习惯（如日本站要用敬语、德国站要更formal）
- 语言检测用的是langdetect库自动识别客户邮件语言

---

## 三、项目二：亚马逊评论自动打标系统（模型训练重点项目）

### 3.1 模型训练全流程故事

> "这个项目最大的技术挑战就是模型训练。一开始我是想直接调API的——用Qwen-Max或者GPT-4o，把500个候选标签全塞进Prompt让它选。但很快发现两个问题：一是token消耗太大，500个标签一次就是3000+ tokens的标签描述，每条评论成本太高；二是大模型其实对我们这种特定业务的标签理解不够，经常把'品质_缺漏件'和'仓储_发错货'搞混。"

> "所以我决定自己微调一个领域模型。选型的时候，我测了4个模型：Qwen2.5-32B、Qwen2.5-72B、QwQ-32B、GLM-4-32B。为什么都是32B以上？因为这个任务需要从50个候选标签里选最合适的1-3个，小模型幻觉太高，经常编造标签库里不存在的标签。"

#### 选型实验结果：

| 模型 | 准确率 | 幻觉率(输出不存在的标签) | 格式遵循率 | 推理速度 |
|------|--------|------------------------|----------|---------|
| Qwen2.5-32B | 83.2% | 8.7% | 91% | 快 |
| Qwen2.5-72B | 89.1% | 3.2% | 97% | 中 |
| QwQ-32B | 85.6% | 5.1% | 88% | 慢（思维链太长） |
| GLM-4-32B | 84.8% | 6.3% | 93% | 中 |

> "QwQ-32B因为是推理模型，会输出很长的思维链，导致推理速度慢而且输出格式不稳定。GLM-4-32B中文理解不错但对我们标签体系的遵循度不如Qwen。最终选了Qwen2.5-72B，虽然参数大，但通过QLoRA量化可以控制显存。"

#### 训练环境与配置：

- **硬件**：4×A100 80GB（公司租的AutoDL云服务器，按时计费）
- **训练框架**：LlamaFactory（选它因为对Qwen支持好，配置简单）
- **第一阶段SFT：**
  - 训练数据：8,000条人工标注样本（从客服团队历史标注数据中筛选高质量的，产品经理协助验证）
  - 数据格式：Alpaca格式，instruction是评论+候选标签列表，output是标签选择结果+理由
  - QLoRA配置：rank=64，alpha=128，target_modules=all（q_proj/k_proj/v_proj/o_proj/gate_proj/up_proj/down_proj）
  - 学习率：2e-5，warmup_ratio=0.1
  - batch_size：每卡2，gradient_accumulation=8，effective batch=64
  - epoch：3
  - 训练时长：大约14小时
  - 显存占用：每卡约65GB（72B模型QLoRA，4bit量化底座+LoRA层float16）
  - checkpoint：每500步保存一次，总共约18个checkpoint，每个约800MB（只保存LoRA权重）
  - 损失曲线：loss从2.8降到0.4左右，第2个epoch后趋于平稳

- **第二阶段DPO：**
  - 数据：2,000条偏好对（正样本=正确标签组合，负样本=易混淆的错误标签组合）
  - 负样本构建：人工整理了50组易混淆标签对（如品质vs仓储、产品vs品质、物流vs仓储），再从SFT模型的错误预测中收集真实的错误案例
  - beta=0.1
  - 学习率：5e-6（比SFT低，DPO对学习率敏感）
  - epoch：1
  - 训练时长：约4小时
  - DPO之后准确率从89%提升到92%+，主要提升在细粒度标签的区分上

#### 训练踩坑故事：

> "训练过程中遇到最大的问题是loss震荡。第一次SFT的时候learning rate用的5e-5，loss在第1个epoch后开始剧烈震荡，上下跳。降到2e-5后稳定了。后来复盘发现是因为训练数据里有一些标注不一致——同样的评论内容，不同客服标的不一样。我后来专门做了一轮数据清洗，把标注一致性低于80%的数据剔除了，重新训练后好很多。"

> "DPO的坑是负样本构建。一开始我是随机抽的负样本，效果很差，因为随机抽的标签和正确标签差异太大，模型太容易区分了，学不到东西。后来改成只用易混淆的标签对作为负样本——就是那些人工也经常搞混的标签，比如'品质_缺漏件_缺电池'和'仓储_漏发_缺少配件'，DPO才开始起作用。"

> "还有一个有意思的发现：我试过直接用SFT训练完的模型做推理，发现它对标签解释库里描述详细的标签效果好，但对那些描述简单的长尾标签效果差。所以我又回去补充了300多条标签的解释文本，重新训练了一版，长尾标签的召回率提升了15%左右。这让我深刻意识到数据质量比模型大小重要得多。"

#### 推理部署：

- **部署方式**：vLLM serving，QLoRA合并后量化为AWQ 4bit
- **硬件**：单卡A100 80GB可以跑72B的AWQ量化版
- **推理显存**：约42GB（AWQ 4bit）
- **吞吐量**：单卡约15 req/s（每个请求平均输入500 tokens、输出100 tokens）
- **月处理量**：1.6万条/月 ≈ 每天约530条，按每条请求2秒算，每天只需要约18分钟就能处理完
- 所以推理成本非常低，一天大部分时间GPU是闲置的，后来把同一张卡也用来跑其他模型

### 3.2 混合检索Pipeline细节

**第一层：规则过滤（代码实现，不涉及模型）**
- 输入：评论对应的SKU
- 逻辑：SKU → 品牌映射 → 过滤已禁用标签 → 过滤不适用该品类的标签
- 输出：约500个候选标签
- 这步是后端同事写的接口，我直接调用

**第二层：Milvus向量检索**
- 事先把3000+标签的解释文本全部用BGE-M3做了Embedding存入Milvus
- 评论文本同样用BGE-M3做Embedding
- 余弦相似度检索Top-50
- 为什么是50？测试过Top-30会漏掉一些正确答案，Top-100噪音太多

**第三层：Neo4j图谱补偿**
- 标签之间有父子关系（品质→品质_缺漏件→品质_缺漏件_缺电池）
- 也有互斥关系（品质_缺漏件 和 仓储_漏发 不能同时出现）
- 向量检索可能只召回了"品质_缺漏件"但漏掉了更具体的"品质_缺漏件_缺电池"，图谱遍历会把子标签也补充进来
- 也会检查互斥关系，在最终精排时去掉不该共存的标签组合

### 3.3 主动学习闭环

- 每天打标结果推送到审核队列
- 人工客服抽检10%（约50条/天），标记为"正确"或"修正"
- 修正的样本自动进入训练数据池
- 每月月底做一次增量训练（在上一版checkpoint基础上继续训练500步），月增约500-1000条高价值样本
- V1→V2→V3，准确率从87%→90%→92.3%

---

## 四、项目三：VOC消费者声音洞察平台

### 4.1 ABSA标签体系设计

> "这个项目的核心不是代码量，而是标签体系的设计。我花了大概两周时间和产品经理一起调研了4个部门的需求，发现大家其实都在分析同样的数据（亚马逊评论），但视角不同。广告部想知道用户怎么描述产品优点（用来写广告文案），产研部想知道用户抱怨什么（用来改进产品），市场部想知道竞品的优劣势。"

> "所以我设计了一个统一的四维度标签体系：Entity是'在说什么东西'（产品、笔头、包装），Aspect是'在说这个东西的什么方面'（颜色、耐用性、手感），Opinion是'情感态度'（正面/负面+强度），Category是'业务上怎么分类'（质量问题/使用建议/竞品对比）。每条评论可能有多个观点，每个观点独立打一组四元组标签。"

### 4.2 CoT推理实现

> "一条评论可能很复杂，比如：'The colors are vibrant but the tips dried out after a week, also the case is flimsy compared to Copic markers'。这里面有三个观点单元：颜色好、笔头不耐用、和竞品对比包装差。"

> "我用的是LangChain的Agent，Prompt里要求模型先做CoT推理：第一步识别有几个独立观点，第二步逐个观点分析四个维度，第三步检查有没有遗漏。输出是严格的JSON格式。通过Few-shot给了10个标注好的复杂评论示例，确保输出一致性。"

### 4.3 HDBSCAN聚类验证

> "标签体系设计好之后，我需要验证标签之间是不是有语义重叠的。用BGE-M3把3000+标签文本都做了Embedding，然后用HDBSCAN聚类。HDBSCAN比DBSCAN好在不需要手动调eps参数，它能自适应不同区域的密度。"

> "聚类结果发现了不少问题：比如'品质_音质_杂音'和'品质_音质_破音'聚在一起了，说明从语义上模型很难区分。还有'产品_尺寸_偏大'和'客户_期望_尺寸不符'也很近。我把这些发现反馈给了产品经理，合并了130+组重叠标签，最终标签体系从3400多个精简到约3000个。"

> "降维可视化用的是UMAP（比t-SNE快而且保持全局结构），用matplotlib画出来给业务方看，让他们直观理解标签空间的分布。"

---

## 五、通用技术问题应对

### Q: "你的RAG效果怎么评估？"
> "我会定期做两个评估：一是离线评估，准备200条标注好的QA对，算检索Top-5的命中率（Hit Rate）和排序质量（MRR）；二是在线评估，看自动回复邮件被客户追问'你没理解我的问题'的比例作为间接指标。我们的Top-5 Hit Rate大概在87%左右，MRR约0.72。"

### Q: "上下文太长怎么办？"
> "分几个层面。第一，知识检索层面，用chunk切分+向量检索，只召回最相关的Top-5，而不是把整个知识库塞进去。第二，对话历史层面，用滑动窗口+摘要压缩，保留最近3轮完整对话+历史摘要。第三，Prompt层面，Skills按需加载，只加载当前品牌的规则。综合下来，输入context控制在4000-6000 tokens。"

### Q: "你怎么评估Agent的效果？"
> "我们有三个核心指标：自动回复占比（35%）、客户满意度CSAT（4.2/5，通过邮件后续调查）、和人工复核修正率（每周抽检100封自动发出的邮件，修正率<5%就达标）。另外通过LangSmith追踪每个Agent节点的延迟和调用成功率，发现瓶颈及时优化。"

### Q: "你训练的模型有没有过拟合？怎么判断的？"
> "有过。第一版SFT训练到第3个epoch的时候，训练集loss还在降但验证集loss开始升了。我最终选的是第2个epoch中间的一个checkpoint（大约是第3000步），这个点验证集loss最低。后来我养成了习惯，每次训练都同时跑eval，用early stopping选checkpoint，而不是一味训到底。"

### Q: "如果你重新做这个项目，会有什么不同？"
> "几个反思：一是数据标注应该更早投入，我们前期花了太多时间调Prompt，后来发现高质量标注数据才是最重要的。二是Agent的复杂度应该渐进式增加，一开始做了很多ReAct循环和Reflection，其实很多场景直接一次LLM调用就能搞定，过度设计反而增加延迟。三是模型选型应该更系统化，早期我在小模型上浪费了不少时间，其实一开始就应该直接测32B以上的模型。"

---

## 六、自我介绍模板（1分钟版）

> "面试官好，我是曾理，目前在深圳千岸科技做AI应用开发工程师，工作了差不多两年多。千岸是国内头部跨境电商品牌公司，我在这里主要负责三个AI项目的从0到1落地。"

> "第一个也是最核心的是智能客服自动回复系统。我们月均处理2万多封客户邮件，我基于LangGraph搭建了一个Multi-Agent架构，实现了35%的邮件全自动回复，响应时间从4小时缩短到15分钟。这个系统里我做了Hybrid RAG（ES+Milvus+Neo4j）、ReAct推理、Reflection自纠错这些。"

> "第二个是评论自动打标系统，这个项目我做了比较深入的模型训练——基于Qwen2.5-72B做了SFT和DPO两阶段微调，准确率做到了92%，完全替代了人工打标。"

> "第三个是VOC消费者洞察平台，设计了一套ABSA四维度标签体系，服务4个业务部门。"

> "之前在成都做过CV和NLP的算法工作，YOLO目标检测、BERT微调这些都有实际项目经验。总体来说我比较偏AI应用层的全栈开发——从模型选型微调到Agent架构设计到RAG搭建到工程部署，都是我一个人在做。"

---

## 七、可能的弱点问题与应对

Q: "你的学历不是985/211，会不会基础不扎实？"
> "确实不是名校出身，但我在专业排前5%，而且实际项目经验比较丰富。我更倾向于通过实际解决问题来学习——比如训练模型遇到loss震荡，就去深入研究学习率调度和数据清洗；做RAG效果不好，就去对比不同Embedding模型和检索策略。我觉得AI应用开发这个领域，动手能力和解决问题的经验比理论知识更关键。"

Q: "你们就3个人的团队，怎么做这么多项目？"
> "因为AI项目和传统开发不一样——AI侧的核心工作量其实集中在架构设计、Prompt工程和模型调优，代码量并不大。智能客服系统Agent核心代码大概3000行Python，但Prompt迭代了几十版。而且三个项目是有时间先后的——客服系统先做，打标系统和VOC是后面并行推进的，打标系统的检索架构很多可以复用客服系统的。"
