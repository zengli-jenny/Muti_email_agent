# 多专家智能客服系统 — 架构分析

## 一、LangGraph 架构技术要点

### 1.1 整体拓扑

```
load_context → router → solver ←→ tool_executor（含知识检索工具）
                           ↓
                  reply_generator → reviewer → END
                                      ↓ (rejected)
                                    solver (带反馈重写)
                                      ↓ (max_reflections)
                                    finalize → END
```

7 个节点，由 `builder.py` 中的 `build_graph()` 函数编排。知识检索不再是固定节点，而是 Solver 按需调用的工具之一。

另有 `build_resume_graph()` 构建从 solver 开始的子图，用于人工介入后断点续跑（跳过 load_context / router）：

```
solver ←→ tool_executor
  ↓
reply_generator → reviewer → END
```

```python
graph = StateGraph(CustomerServiceState)
graph.set_entry_point("load_context")
graph.add_edge("load_context", "router")
graph.add_edge("router", "retriever")
graph.add_edge("retriever", "solver")
# solver 条件分支 → call_tool / generate_reply / need_human
# tool_executor → solver（循环）
# reviewer 条件分支 → approved(END) / rejected(solver) / max_reflections(finalize)
graph.add_edge("finalize", END)
```

### 1.2 状态管理（State）

定义在 `smart_customer_service/graph/state.py`，使用 `TypedDict` + LangGraph 的 `Annotated` reducer：

```python
class CustomerServiceState(TypedDict, total=False):
    # 输入
    customer_email: str
    brand: str
    subject: str
    body: str
    old_emails: str

    # 上下文加载
    memory_facts: list[str]
    skill_profile: dict          # ← 品牌 Skill 在这里
    detected_language: str

    # 路由
    basic_info: dict
    selected_policy: str
    policy_content: str

    # 知识检索
    retrieved_knowledge: str

    # ReAct 循环
    thought_history: Annotated[list[dict], _append_list]   # 自动追加
    react_iteration: int
    solver_decision: str         # "call_tool" | "generate_reply" | "need_human"
    pending_tool_call: dict
    tool_results: Annotated[dict, _merge_dicts]            # 自动合并

    # 回复生成
    draft_reply: str
    reply_type: str
    human_tasks: list[dict]

    # 反思循环
    review_feedback: str
    review_passed: bool
    reflection_count: int
    max_reflections: int

    # 输出
    final_reply: str
    requires_human: bool

    # 追踪
    trace_log: Annotated[list[dict], _append_list]         # 自动追加
```

关键设计：
- `Annotated[list, _append_list]`：每个节点返回的 `thought_history` 和 `trace_log` 会自动追加到已有列表，不会覆盖
- `Annotated[dict, _merge_dicts]`：`tool_results` 会自动合并，多次工具调用的结果累积保留
- 普通字段（如 `solver_decision`、`draft_reply`）是覆盖式更新

### 1.3 条件边（Conditional Edges）

定义在 `smart_customer_service/graph/edges.py`：

**Solver 出口**：`decide_solver_next(state)`
- 如果 `react_iteration >= max_react_iterations`（默认 7）→ 强制 `need_human`
- 否则按 `solver_decision` 字段路由：`call_tool` / `generate_reply` / `need_human`

**Reviewer 出口**：`decide_review_result(state)`
- `review_passed == True` → `approved`（结束）
- `reflection_count >= max_reflections`（默认 2）→ `max_reflections`（走 finalize）
- 否则 → `rejected`（回到 solver 重写）

### 1.4 节点工厂（NodeFactory）

定义在 `smart_customer_service/graph/nodes.py`，通过依赖注入模式创建：

```python
class NodeFactory:
    def __init__(self, llm, memory_store, skill_loader, policy_loader,
                 knowledge_retriever, tool_registry, rule_router, policy_routing_text):
        ...
```

每个节点是 `NodeFactory` 的一个异步方法，接收 `state` 返回 `dict`（部分状态更新）。

---

## 二、提示词体系

### 2.1 提示词文件位置

所有 prompt 模板集中在一个文件：`smart_customer_service/prompt_templates.py`

### 2.2 每个节点的提示词

| 节点 | 变量名 | 行号 | 作用 |
|------|--------|------|------|
| Router | `ROUTER_SYSTEM` | L11-49 | 识别客诉场景，提取 basic_info，匹配标准处理流程 |
| Solver | `SOLVER_SYSTEM` | L63-98 | ReAct 推理核心：思考→决策→工具调用/生成回复/转人工 |
| Reply Generator | `REPLY_GENERATOR_SYSTEM` | L167-183 | 把草稿格式化为完整客服邮件（称呼、语气、签名） |
| Reviewer | `REVIEWER_SYSTEM` | L190-215 | 审核事实一致性、方案合规性、品牌调性 |

### 2.3 动态构建函数

- `build_router_user_prompt(body, old_emails, subject)` (L52)
  拼接邮件正文 + 历史邮件，作为 Router 的 user message

- `build_solver_user_prompt(body, old_emails, subject, retrieved_knowledge, tool_results, thought_history, review_feedback, memory_facts)` (L101)
  拼接邮件、知识库结果、工具返回、推理历史、审核反馈，作为 Solver 的 user message

- `build_solver_system(skill_raw_markdown, policy_content, max_iterations)` (L147)
  把品牌 Skill、标准流程、工具列表注入 Solver 的 system prompt

### 2.4 运行时注入的内容

| 注入内容 | 来源文件 | 注入到哪些节点 |
|----------|----------|----------------|
| 品牌 Skill | `skills/ohuhu.md`、`skills/tribit.md` | Solver、Reply Generator、Reviewer |
| 标准流程 | `标准流程/*.md`（15个文件） | Solver、Reviewer |
| 政策路由规则 | `政策路由.md` | Router 的 `{policy_routing_rules}` 占位符 |
| 工具描述 | `tool_registry.py` 的 `TOOL_DESCRIPTIONS` | Solver 的 `{tool_list}` 占位符 |
| 知识库检索结果 | `data/knowledge_base_full.json`（1546条） | Solver 的 user prompt |
| 客户记忆 | `data/memory.json` | Solver 的 user prompt |

模板本身约 2000 字符，加上运行时注入的标准流程、知识库、工具返回等，实际每次 LLM 调用可能到几千甚至上万字符。

---

## 三、Skills 架构分析

### 3.1 ohuhu.md 和 tribit.md 是什么

品牌人格配置文件，本质上是 LLM 的行为约束卡片。每个文件定义了：

| 字段 | ohuhu | tribit |
|------|-------|--------|
| Tone | warm and supportive | professional and concise |
| Greeting | Hi there, | Hello, |
| Closing | Warm regards, | Best regards, |
| 退款审批阈值 | $50 | $50 |
| Rules | 提及订单号、包裹签收先让客户自查、超阈值转人工 | 精确产品/保修信息、换货前查库存、超阈值转人工 |
| ReplyStyle | 短段落、直接给方案、共情措辞 | 正式语气、事实先行 |

### 3.2 为什么只有这两个品牌

`skills.py` 的 `SkillLoader.load()` 逻辑：

```python
skill_path = self.skills_dir / f"{brand.lower()}.md"
if not skill_path.exists():
    return SkillProfile(brand=brand, tone="clear and professional", ...)
```

按需加载 — 传入什么 brand 就去 `skills/` 目录找对应的 `.md` 文件。找不到就返回通用默认 profile。只有两个文件是因为只写了两个品牌的配置，不是架构限制。有多少个品牌，就加多少个 `{brand}.md` 文件即可。

### 3.3 项目是否需要这两个 Skills

需要，而且是核心积木之一。Skills 被注入到 LangGraph 工作流的 4 个关键节点中：

- `load_context` — 加载品牌 profile，决定语气/称呼/规则
- `solver` — 品牌 Skill 的 raw_markdown 直接写入 ReAct system prompt，约束 LLM 的决策行为
- `reply_generator` — 用 tone/greeting/closing 控制邮件格式
- `reviewer` — 用 skill_profile 审核品牌调性合规

没有 Skill 文件时，所有品牌退化成同一个 "generic professional" 人格，丢失品牌差异化。

### 3.4 Skill 注入数据流

```
load_context ──→ skill_profile 写入 State
                      │
                      ├──→ solver (system prompt 包含 skill raw_markdown)
                      │         → 影响 ReAct 推理决策
                      │
                      ├──→ reply_generator (tone/greeting/closing)
                      │         → 控制邮件格式化
                      │
                      └──→ reviewer (skill_profile 做品牌调性审核)
                                → 不合规则打回重写
```

Skill 在 `load_context`（第 1 个节点）就加载，然后贯穿整个链路。

### 3.5 多品牌扩展方案

当前架构已经是积木化的，扩展只需要：

1. 在 `skills/` 目录下新增品牌文件，如 `skills/anker.md`、`skills/baseus.md`，格式和 ohuhu.md 一样
2. 前端请求时传入 `brand` 字段（API 的 ReplyPayload 已支持）
3. 后端自动按 brand 名加载对应 Skill，无需改任何代码

---

## 四、工具注册表（Tool Registry）

定义在 `smart_customer_service/tool_registry.py`，共 12 个工具：

| 工具名 | 功能 | 参数 |
|--------|------|------|
| 查询订单基本信息 | 获取订单渠道、平台、国家、状态、物流号、SKU | order_id |
| 查询订单补充信息 | 获取货值、保修周期、是否在保 | order_id, sku, end_date |
| 查询关联订单基本信息 | 查询关联订单 | order_id |
| 查询拆单订单号 | 查询拆单子订单号 | order_id |
| 查询物流信息 | 物流状态、更新时间、跟踪链接 | tracking_number, carrier_code? |
| 查询库存信息 | 产品库存查询 | sale_type, sku, channel, country_code? |
| 通过官网链接查询产品SKU | 官网链接→SKU | url |
| 通过ASIN查询SKU | Amazon ASIN→SKU | asin |
| 通过SKU查询产品信息 | SKU→型号、品线、系列、色号 | sku |
| TSM客服邮箱查询工具 | 邮箱→渠道信息 | email |
| 通过品牌查询渠道 | 品牌→渠道列表 | brand |
| 知识检索工具 | 搜索产品FAQ知识库 | query, brand?, product_model? |

工具描述通过 `TOOL_DESCRIPTIONS` 字典注入到 Solver 的 system prompt 中，LLM 根据描述决定调用哪个工具。

---

## 五、提示词调整指南（代码级）

前端 Settings 页面可以覆盖 4 个节点（Router / Solver / Reply Generator / Reviewer）的完整 prompt。但如果你要调细节，需要直接改代码。下面按"你想调什么"分类，告诉你去哪里改。

### 5.1 调整 4 个核心 Agent 的 System Prompt 模板

全部在 `smart_customer_service/prompt_templates.py` 一个文件里：

| 你想调什么 | 改哪个变量 | 行号 | 说明 |
|-----------|-----------|------|------|
| Router 如何识别客诉、提取 basic_info 的指令 | `ROUTER_SYSTEM` | L11-49 | 包含角色设定、输入说明、任务指令、输出 JSON 格式。`{policy_routing_rules}` 占位符会被 `政策路由.md` 的内容替换 |
| Solver 的 ReAct 推理行为原则 | `SOLVER_SYSTEM` | L63-98 | 包含角色设定、行为原则（6条）、输出 JSON 格式。`{skill_profile}` `{policy_content}` `{tool_list}` `{max_iterations}` 是运行时占位符 |
| Reply Generator 的邮件格式化要求 | `REPLY_GENERATOR_SYSTEM` | L167-183 | 包含称呼、正文、敬语、签名要求。`{detected_language}` `{tone}` `{greeting}` `{closing}` `{reply_content}` 是运行时占位符 |
| Reviewer 的审核维度和标准 | `REVIEWER_SYSTEM` | L190-215 | 三个审核维度：事实一致性、方案合规性、品牌调性。`{policy_content}` `{skill_profile}` `{tool_results}` `{draft_reply}` 是运行时占位符 |

### 5.2 调整 Solver 的 User Prompt 拼接逻辑

当你想改变"传给 Solver 的上下文包含哪些内容、以什么顺序排列"时：

| 你想调什么 | 改哪个函数 | 文件 | 行号 |
|-----------|-----------|------|------|
| Solver 收到的用户消息里包含哪些段落（邮件、知识库、工具结果、推理历史、审核反馈、客户记忆）的拼接顺序和格式 | `build_solver_user_prompt()` | `prompt_templates.py` | L101-144 |
| Solver 的 system prompt 里品牌 Skill、标准流程、工具列表的注入方式 | `build_solver_system()` | `prompt_templates.py` | L147-160 |
| Router 收到的用户消息里邮件正文和历史邮件的拼接格式 | `build_router_user_prompt()` | `prompt_templates.py` | L52-56 |

### 5.3 调整品牌人格（Skill）— 不改代码，改 Markdown 文件

| 你想调什么 | 改哪个文件 | 说明 |
|-----------|-----------|------|
| ohuhu 品牌的语气、称呼、规则、回复风格 | `skills/ohuhu.md` | 直接编辑 Markdown，格式：顶部 key:value 元数据 + `## Rules` + `## ReplyStyle` 段落 |
| tribit 品牌的语气、称呼、规则、回复风格 | `skills/tribit.md` | 同上 |
| 新增品牌 | `skills/{brand}.md` | 新建文件，格式同 ohuhu.md，后端自动按 brand 名加载 |
| Skill 的解析逻辑（比如新增字段） | `smart_customer_service/skills.py` | `SkillLoader._parse_markdown()` 方法，L43-75 |

### 5.4 调整政策路由规则 — 不改代码，改 Markdown 文件

| 你想调什么 | 改哪个文件 | 说明 |
|-----------|-----------|------|
| LLM Router 看到的路由规则（哪些场景匹配哪个流程） | `政策路由.md` | 整个文件内容会被注入到 `ROUTER_SYSTEM` 的 `{policy_routing_rules}` 占位符 |
| 规则路由兜底的关键词匹配逻辑（LLM 路由失败时的 fallback） | `smart_customer_service/router_agent.py` | 每个 `_is_xxx()` 方法里的 `keywords` 列表，L142-215 |

### 5.5 调整标准处理流程 — 不改代码，改 Markdown 文件

| 你想调什么 | 改哪个文件 | 说明 |
|-----------|-----------|------|
| 某个场景的具体处理步骤（如物流问题怎么处理） | `标准流程/物流问题处理流程.md` 等 15 个文件 | 整个文件内容会被注入到 Solver 和 Reviewer 的 `{policy_content}` 占位符 |
| 新增一个处理流程 | `标准流程/xxx处理流程.md` | 新建文件，同时在 `政策路由.md` 里添加对应的路由规则 |

### 5.6 调整工具描述（影响 Solver 选择调用哪个工具）

| 你想调什么 | 改哪个文件 | 行号 | 说明 |
|-----------|-----------|------|------|
| 工具的名称和描述文案（LLM 根据这些描述决定调什么工具） | `smart_customer_service/tool_registry.py` | L111-124 `TOOL_DESCRIPTIONS` 字典 | 改 value 影响 LLM 对工具的理解；改 key 需要同步改 `_TOOL_DISPATCH`（L95-108） |

### 5.7 调整节点内部的硬编码行为（nodes.py）

这些不是 prompt 模板，但直接影响 LLM 的输入输出处理：

| 你想调什么 | 改哪个位置 | 文件:行号 | 说明 |
|-----------|-----------|----------|------|
| 语言检测逻辑（影响回复语言） | `_detect_language()` | `nodes.py:454-470` | 简单启发式：CJK 字符占比 > 10% 判中文，关键词判法/西/德语，其余默认英语 |
| 邮件是否已格式化的判断（跳过 Reply Generator） | `_looks_like_complete_email()` | `nodes.py:473-482` | 检查开头有 greeting、结尾有 closing 就认为已格式化 |
| 人工指令注入后强制跳过 need_human | `solver()` 方法内 | `nodes.py:266-271` | 检测 `[人工客服指令]` 标记，如果已有人工指令就强制走 `generate_reply` |
| LLM JSON 输出的解析容错 | `_parse_json_from_llm()` | `nodes.py:39-73` | 处理 markdown code fence、Python True/False/None、嵌套 JSON 提取 |
| 前端自定义 prompt 覆盖机制 | `_get_custom_prompt()` | `nodes.py:149-152` | 从 `state.node_config[node_name].custom_prompt` 读取，非空则完全替换默认模板 |

### 5.8 调整知识库检索（影响 Solver 看到的 FAQ 内容）

| 你想调什么 | 改哪个文件 | 说明 |
|-----------|-----------|------|
| FAQ 知识库内容 | `data/knowledge_base_full.json` | 1546 条 FAQ，直接编辑 JSON |
| 检索算法（BM25 + TF-IDF）、返回条数 | `smart_customer_service/knowledge_retriever.py` | `search_as_text()` 方法，默认 `top_k=5` |

### 5.9 总结：按修改难度排序

**只改文件内容（零代码）：**
1. `skills/*.md` — 品牌人格
2. `标准流程/*.md` — 处理流程
3. `政策路由.md` — 路由规则
4. `data/knowledge_base_full.json` — FAQ 知识库

**改 Python 模板字符串（低风险）：**
5. `prompt_templates.py` 的 4 个 `*_SYSTEM` 变量 — 核心 prompt
6. `prompt_templates.py` 的 3 个 `build_*` 函数 — 上下文拼接逻辑
7. `tool_registry.py` 的 `TOOL_DESCRIPTIONS` — 工具描述

**改 Python 逻辑（需谨慎）：**
8. `nodes.py` 的辅助函数 — 语言检测、格式判断、JSON 解析
9. `router_agent.py` 的关键词列表 — 规则路由兜底
10. `skills.py` 的 `_parse_markdown()` — Skill 解析逻辑

---

## 六、关键文件索引

| 文件路径 | 作用 |
|----------|------|
| `smart_customer_service/graph/builder.py` | LangGraph 图编排，定义节点和边 |
| `smart_customer_service/graph/state.py` | 状态定义（TypedDict + Annotated reducer） |
| `smart_customer_service/graph/edges.py` | 条件边函数（solver 出口、reviewer 出口） |
| `smart_customer_service/graph/nodes.py` | 8 个节点的具体实现（NodeFactory） |
| `smart_customer_service/prompt_templates.py` | 所有 LLM 提示词模板 |
| `smart_customer_service/skills.py` | 品牌 Skill 加载器（SkillLoader） |
| `smart_customer_service/tool_registry.py` | 工具注册表（12 个工具） |
| `smart_customer_service/knowledge_retriever.py` | BM25 + TF-IDF 知识库检索 |
| `smart_customer_service/policy_loader.py` | 标准流程加载器（15 个流程文件） |
| `smart_customer_service/router_agent.py` | 规则路由兜底（LLM 路由失败时） |
| `smart_customer_service/tcs_client.py` | TCS API 客户端 |
| `smart_customer_service/memory.py` | 客户记忆存储 |
| `smart_customer_service/config.py` | 应用配置（从 .env 读取） |
| `skills/ohuhu.md` | ohuhu 品牌人格配置 |
| `skills/tribit.md` | tribit 品牌人格配置 |
| `标准流程/*.md` | 15 个标准客服处理流程 |
| `政策路由.md` | 政策路由规则（注入 Router prompt） |
| `data/knowledge_base_full.json` | 1546 条 FAQ 知识库 |
| `api/main.py` | FastAPI 入口，SSE 流式接口 |

---

## 七、架构重构技术报告：渐进式技能披露（Progressive Skill Disclosure）

> 改造日期：2026-03-23
> 改造范围：后端 Agent 架构 + 前端管理界面

---

### 7.1 改造背景与问题诊断

#### 原架构的核心问题

原系统采用 **Router Agent + 全量策略注入** 的设计模式：

```
load_context → router → solver ←→ tool_executor
```

Router 节点每次都要：
1. 消耗一次完整 LLM 调用（~600 tokens 的 `ROUTER_SYSTEM` + 政策路由规则全文）
2. 将匹配到的标准流程文件**全文**注入 Solver 的 system prompt

以物流问题处理流程为例，该文件约 **14,500 字符**，每次 Solver 迭代都要携带这些内容。一个典型的 7 轮 ReAct 循环，这份流程文件会被重复传输 7 次。

**实测 token 消耗分析（改造前）：**

| 组件 | 每次调用 token 估算 |
|------|-------------------|
| Router system prompt（含政策路由规则全文） | ~3,500 tokens |
| Solver system prompt（含完整标准流程） | ~4,000–6,000 tokens |
| 每轮 ReAct 迭代（×7） | 重复携带流程内容 |
| **总计（单次邮件处理）** | **~35,000–50,000 tokens** |

这带来两个问题：
- **成本高**：大量 token 浪费在重复传输 LLM 已经"看过"的内容
- **架构冗余**：Router 是一个独立 LLM 调用，但它做的事（意图识别 + 流程匹配）完全可以由 Solver 在 ReAct 循环的第一步完成

#### 参考设计：Anthropic 的 Tool-Use 最佳实践

Anthropic 在 Claude 的 Agent 设计中推荐的模式是：**不要预先注入所有上下文，而是让 Agent 通过工具调用按需获取**。这与 RAG（检索增强生成）的思路一致——只在需要时加载，而不是一次性全量注入。

---

### 7.2 设计方案：三级渐进式披露

核心思想：将 15 个标准处理流程按"使用频率"和"细节程度"分为三层，每层的 token 成本递增，但只在真正需要时才加载。

```
L1  始终可见（~750 tokens）
    ↓ Solver 识别意图后调用 load_skill(skill_id)
L2  按需加载（~200–800 tokens）
    ↓ 遇到复杂步骤时调用 load_skill_section(skill_id, section)
L3  深度按需（~2,000–14,500 tokens）
```

#### L1：技能注册表（Skill Table）

始终注入到 Solver 的 system prompt，替代原来的 Router。格式为 Markdown 表格，每行约 50 tokens：

```markdown
| ID | 技能名称 | 触发条件 | 说明 |
|---|---|---|---|
| logistics | 物流问题处理 | 物流状态异常、包裹未收到... | 处理运输过程中的物流异常... |
| quality | 商品质量问题处理 | 产品功能故障、外观缺陷... | 处理非马克笔类产品的质量缺陷... |
| ... | ... | ... | ... |
```

15 个技能 × 约 50 tokens = **~750 tokens 固定开销**，替代原来 3,500 tokens 的 Router 调用。

#### L2：核心规则（总纲章节）

每个标准流程文件的"总纲"部分，包含最关键的业务规则和判断前提。通常 200–800 tokens。Solver 在识别意图后，通过 `load_skill(skill_id)` 工具调用获取。

#### L3：完整 SOP（按章节加载）

标准流程的详细步骤、话术模板、边界条件。通过 `load_skill_section(skill_id, section)` 按需加载特定章节，如"换货条件判断流程"、"执行退款"等。

---

### 7.3 改造内容详解

#### 7.3.1 新增文件

**`skills/skill_registry.json`**

技能注册表的数据源，定义每个技能的三层结构：

```json
{
  "skills": [
    {
      "id": "logistics",
      "name": "物流问题处理",
      "file": "物流问题处理流程.md",
      "trigger": "物流状态异常、包裹未收到、物流信息未更新、包裹无法投递",
      "l1_summary": "处理运输过程中的物流异常、包裹丢失、未投递等问题",
      "l2_section": "总纲",
      "l3_sections": ["Step 1", "Step 2", "Step 3", "官网平台处理流程", "换货条件判断流程", ...]
    }
  ]
}
```

**`smart_customer_service/skill_registry.py`**

`SkillRegistry` 类，负责：
- 从 JSON 加载注册表
- `build_l1_table()` — 生成注入 Solver 的 L1 表格字符串
- `load_skill(skill_id, level)` — 加载 L2 或 L3 内容
- `load_skill_section(skill_id, section)` — 加载特定 L3 章节
- CRUD 接口供前端管理 UI 调用

内容提取使用正则匹配 Markdown 标题，支持 `#`、`##`、`###` 三级标题的章节定位。

#### 7.3.2 修改文件

**`smart_customer_service/tool_registry.py`**

新增两个工具，注册到 `_TOOL_DISPATCH` 和 `TOOL_DESCRIPTIONS`：

```python
"load_skill": "【必须首先调用】根据 skill_id 加载标准处理流程内容。
               level=2（默认）加载核心规则（总纲），level=3 加载完整流程。"

"load_skill_section": "加载某个标准处理流程的特定章节（L3 级别）。
                       当 load_skill(level=2) 的内容不足以处理当前问题时，按需加载。"
```

工具总数从 12 个增加到 **14 个**。

**`smart_customer_service/prompt_templates.py`**

删除 `ROUTER_SYSTEM` 和 `build_router_user_prompt()`。

`SOLVER_SYSTEM` 新增两个注入点：
- `{skill_table}` — L1 技能注册表（始终可见）
- 行为原则第一条改为：**"必须先识别客户问题类型，调用 load_skill 加载对应的标准处理流程，再做决策"**

`build_solver_system()` 新增 `skill_table` 参数。

**`smart_customer_service/graph/nodes.py`**

`NodeFactory` 构造函数签名变更：

```python
# 改造前
NodeFactory(llm, memory_store, skill_loader, policy_loader,
            knowledge_retriever, tool_registry, rule_router, policy_routing_text)

# 改造后
NodeFactory(llm, memory_store, skill_loader, skill_registry,
            knowledge_retriever, tool_registry)
```

移除 `router` 节点方法，`load_context` 节点新增 `skill_table` 字段写入 State。

`tool_executor` 节点新增逻辑：当工具名为 `load_skill` 或 `load_skill_section` 时，将返回的流程内容**累积追加**到 `policy_content` 字段，而不是简单存入 `tool_results`。这样 Reviewer 节点可以直接读取已加载的流程内容进行合规审核。

**`smart_customer_service/graph/builder.py`**

图拓扑从 7 节点简化为 **6 节点**：

```
# 改造前（7节点）
load_context → router → solver ←→ tool_executor → reply_generator → reviewer → finalize

# 改造后（6节点）
load_context → solver ←→ tool_executor → reply_generator → reviewer → finalize
```

**`smart_customer_service/graph/state.py`**

新增两个状态字段：
- `skill_table: str` — L1 表格，由 `load_context` 写入，`solver` 读取
- `policy_content: str` — 累积的 L2/L3 内容，由 `tool_executor` 追加写入

**`api/main.py`**

移除 `RouterAgent`、`policy_routing_text` 的初始化逻辑。

新增 5 个 Skills 管理 API 端点：

```
GET    /api/skills                  列出所有技能（L1 元数据）
GET    /api/skills/{id}             获取技能详情（含 L2 内容）
PATCH  /api/skills/{id}             更新注册表元数据
PUT    /api/skills/{id}/content     更新流程文件内容
GET    /api/skills/meta/l1_table    获取 L1 表格预览
```

**`api/models.py`**

新增两个请求模型：
- `SkillEntryUpdate` — 技能元数据的部分更新（name、trigger、l1_summary 等）
- `SkillFileUpdate` — 流程文件内容的全量替换

`ResumeRequest` 新增 `policy_content` 字段，确保人工介入后断点续跑时已加载的流程内容不丢失。

---

### 7.4 前端改造

#### 新增页面：技能注册表（SkillsPage）

路径：`web/src/pages/SkillsPage.tsx`

功能设计：
- **左侧列表**：展示所有 15 个技能，显示 ID、名称、一句话说明
- **右侧详情**：三层结构可视化
  - L1 卡片：触发条件 + 一句话说明（始终注入，约 50 tokens）
  - L2 卡片：可展开查看总纲内容（按需加载）
  - L3 卡片：展示所有可按需加载的章节名列表
- **编辑功能**：
  - "编辑元数据"— 修改 L1 触发条件、说明、L2 章节名、L3 章节列表
  - "编辑流程文件"— 直接编辑对应的 `.md` 文件内容，支持预览/编辑切换
- **L1 注入预览**：顶部可展开查看当前实际注入到 Solver 的 L1 表格内容

#### 导航更新

`Sidebar.tsx` 新增"技能注册表"导航项（BookOpen 图标），位于"管理"分组下。

`App.tsx` 新增 `case 'skills': return <SkillsPage />`。

`useStore.ts` 的 `View` 类型新增 `'skills'`。

#### 设置页面清理

`SettingsPage.tsx`：
- 移除 Router 提示词 Tab
- 移除 Router 深度思考开关
- 默认激活 Tab 从 `router` 改为 `solver`

#### SSE 流式展示更新

`useSSE.ts`：
- 移除 `router` 节点的 `NODE_META` 和 `STREAM_LABELS` 定义
- `tool_executor` 节点描述更新为"调用 TCS API / 知识库检索 / 加载技能流程"
- Solver 节点完成时，若 `selected_policy` 有值，subtitle 显示"已加载技能: xxx"
- `buildBlocks()` 中，当 action 包含 `load_skill` 时，label 显示"加载技能流程"而非"决策"

---

### 7.5 改造效果对比

#### Token 消耗对比

| 场景 | 改造前 | 改造后 | 节省 |
|------|--------|--------|------|
| Router 调用 | ~3,500 tokens | 0（已删除） | 100% |
| Solver 首轮（含完整流程） | ~5,000 tokens | ~2,500 tokens（L1表格+L2总纲） | ~50% |
| Solver 后续轮次 | 重复携带完整流程 | 仅携带已加载内容 | ~60–80% |
| 简单问题（无需 L3） | 全量注入 | 仅 L1+L2 | ~70% |
| 复杂问题（需要 L3） | 全量注入 | L1+L2+按需 L3 章节 | ~30–50% |

#### 架构复杂度对比

| 维度 | 改造前 | 改造后 |
|------|--------|--------|
| 图节点数 | 7 | 6 |
| LLM 调用次数（最少） | 3（Router+Solver+Reviewer） | 2（Solver+Reviewer） |
| 新增工具数 | 12 | 14（+load_skill, +load_skill_section） |
| 可配置入口 | 4 个 prompt 模板 | 4 个 prompt 模板 + 技能注册表 UI |

#### 可维护性提升

改造前，新增或修改一个标准处理流程需要：
1. 在 `标准流程/` 目录新建 `.md` 文件
2. 在 `政策路由.md` 中添加路由规则
3. 重启服务

改造后，新增流程只需：
1. 在 `标准流程/` 目录新建 `.md` 文件
2. 在 `skills/skill_registry.json` 中添加一条记录（或通过前端 UI 操作）
3. 重启服务（或调用 `skill_registry.reload()`）

---

### 7.6 设计权衡与注意事项

#### 权衡点 1：Solver 首轮必须调用 load_skill

改造后，Solver 的第一轮 ReAct 迭代必须调用 `load_skill` 才能获取处理流程。这意味着：
- 最少需要 **2 轮 ReAct 迭代**（第1轮加载技能，第2轮开始处理）
- 对于极简单的问题（如"问题已解决回复"），原来 Router 直接匹配后 Solver 一轮就能生成回复，现在需要两轮

这是用**一次额外的工具调用**换取**整体 token 大幅减少**的合理权衡。

#### 权衡点 2：policy_content 的累积机制

`tool_executor` 节点在处理 `load_skill` 调用时，会将内容**追加**到 `policy_content` 字段。这意味着：
- 如果 Solver 多次调用 `load_skill`（如先加载 L2，再加载 L3），内容会累积
- Reviewer 节点读取的是累积后的完整内容，审核准确性不受影响
- 极端情况下（Solver 加载了多个技能），`policy_content` 可能较长，但这种情况本身说明问题复杂，需要更多上下文

#### 权衡点 3：L2 章节提取的准确性

`SkillRegistry._extract_section()` 使用正则匹配 Markdown 标题来提取 L2 章节。当前 15 个流程文件的"总纲"章节都以 `# 总纲` 开头，提取准确。

如果未来新增的流程文件没有"总纲"章节，会自动 fallback 到 `_extract_first_section()`（提取第一个顶级标题下的内容）。

---

### 7.7 改造后的完整文件索引

| 文件路径 | 状态 | 说明 |
|----------|------|------|
| `skills/skill_registry.json` | **新增** | 15 个技能的 L1/L2/L3 注册表 |
| `smart_customer_service/skill_registry.py` | **新增** | SkillRegistry 类（渐进式加载核心） |
| `web/src/pages/SkillsPage.tsx` | **新增** | 技能注册表管理 UI |
| `smart_customer_service/prompt_templates.py` | **修改** | 删除 ROUTER_SYSTEM，Solver 新增 skill_table 注入 |
| `smart_customer_service/tool_registry.py` | **修改** | 新增 load_skill、load_skill_section 工具 |
| `smart_customer_service/graph/nodes.py` | **修改** | 删除 router 节点，NodeFactory 构造函数更新 |
| `smart_customer_service/graph/builder.py` | **修改** | 图拓扑从 7 节点改为 6 节点 |
| `smart_customer_service/graph/state.py` | **修改** | 新增 skill_table、policy_content 字段 |
| `smart_customer_service/config.py` | **修改** | 新增 skill_registry_file 配置项 |
| `api/main.py` | **修改** | 移除 RouterAgent，新增 5 个 Skills API 端点 |
| `api/models.py` | **修改** | 新增 SkillEntryUpdate、SkillFileUpdate 模型 |
| `web/src/App.tsx` | **修改** | 新增 skills 路由 |
| `web/src/components/layout/Sidebar.tsx` | **修改** | 新增技能注册表导航项 |
| `web/src/hooks/useSSE.ts` | **修改** | 移除 router 节点，更新技能加载展示逻辑 |
| `web/src/pages/SettingsPage.tsx` | **修改** | 移除 Router 相关配置项 |
| `web/src/pages/AboutPage.tsx` | **修改** | 更新架构图和 Agent 说明 |
| `web/src/store/useStore.ts` | **修改** | 新增 skills 视图类型，policy_content 字段 |
| `web/src/lib/api.ts` | **修改** | ResumePayload 新增 policy_content |
| `smart_customer_service/router_agent.py` | **保留** | 未删除，可作为兜底或未来参考 |
| `政策路由.md` | **保留** | 未删除，可作为历史参考 |
