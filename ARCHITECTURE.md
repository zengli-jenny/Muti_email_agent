# 跨境电商智能客服系统 - 多专家 Agent 架构设计

## 设计理念

基于 Anthropic 的最佳实践,采用**分层专家 Agent 架构**,将复杂的客服流程分解为:
- **协调层**: 整体流程编排、人工介入判断
- **路由层**: 根据政策路由规则分发到正确的专家
- **专家层**: 15 个专家 Agent,每个专注一个标准流程
- **工具层**: 统一的 API 调用接口

## 架构优势

1. **可维护性**: 每个专家 Agent 的提示词控制在 2000 tokens 以内
2. **可扩展性**: 新增流程只需添加新的专家 Agent
3. **可调试性**: 每个 Agent 独立测试和优化
4. **性能优化**: 可以为不同专家选择不同的模型(Opus/Sonnet/Haiku)
5. **并行处理**: 多个意图可以并行调用不同专家

## 三层架构详解

### 第一层: 协调 Agent (Coordinator Agent)

**职责**:
- 接收用户邮件,提取基本信息(BasicInfo)
- 调用路由 Agent 获取适用的标准流程
- 编排专家 Agent 的执行顺序
- 判断是否需要人工介入
- 整合多个专家的输出,生成最终回复

**输入**:
```json
{
  "OldEmail": "邮件原文及历史",
  "customer_email": "客户邮箱",
  "brand": "品牌",
  "subject": "邮件主题",
  "body": "邮件正文"
}
```

**输出**:
```json
{
  "selected_policies": ["物流问题处理流程.md"],
  "expert_results": [...],
  "final_reply": "最终邮件回复",
  "requires_human": false,
  "human_tasks": []
}
```

### 第二层: 路由 Agent (Router Agent)

**职责**:
- 根据 `政策路由.md` 的规则判断适用的标准流程
- 支持多意图识别(一封邮件可能涉及多个问题)
- 返回优先级排序的流程列表

**输入**:
```json
{
  "OldEmail": "邮件原文",
  "BasicInfo": {
    "order_id": "订单号",
    "issue_type": "问题类型",
    "order_status": "订单状态"
  }
}
```

**输出**:
```json
{
  "PoliciesFileName": [
    "物流问题处理流程.md",
    "商品质量问题处理流程.md"
  ]
}
```

### 第三层: 专家 Agent Pool (15 个专家)

每个专家 Agent 对应一个标准流程文件,专注处理该领域的问题。

#### 专家 Agent 列表

1. **物流问题专家** (`LogisticsExpert`)
   - 流程文件: `物流问题处理流程.md`
   - 工具: 查询订单基本信息、查询物流信息、查询库存信息

2. **商品质量问题专家** (`QualityExpert`)
   - 流程文件: `商品质量问题处理流程.md`
   - 工具: 查询订单基本信息、查询订单补充信息、查询库存信息、知识检索工具

3. **马克笔质量问题专家** (`MarkerQualityExpert`)
   - 流程文件: `马克笔质量问题处理流程.md`
   - 工具: 查询订单基本信息、查询订单补充信息、查询包含部分问题色号套装、查询同系列马克笔套装、查询包含所有问题色号套装

4. **售前咨询专家** (`PreSalesExpert`)
   - 流程文件: `售前咨询处理流程.md`
   - 工具: 通过官网链接查询产品SKU、通过ASIN查询SKU、TSM客服邮箱查询工具、通过品牌查询渠道

5. **售后咨询专家** (`AfterSalesExpert`)
   - 流程文件: `售后咨询处理流程.md`
   - 工具: 查询订单基本信息、查询物流信息、知识检索工具

6. **发货差错问题专家** (`ShippingErrorExpert`)
   - 流程文件: `发货差错问题处理流程.md`
   - 工具: 查询订单基本信息、查询库存信息

7. **设计与描述差异问题专家** (`DesignDescriptionExpert`)
   - 流程文件: `设计与描述差异问题处理流程.md`
   - 工具: 查询订单基本信息、查询订单补充信息

8. **客户原因退换货专家** (`CustomerReturnExpert`)
   - 流程文件: `客户原因退换货处理流程.md`
   - 工具: 查询订单基本信息、查询订单补充信息、查询库存信息

9. **订单修改与取消专家** (`OrderModificationExpert`)
   - 流程文件: `订单修改与取消流程.md`
   - 工具: 查询订单基本信息

10. **红人要样或合作专家** (`InfluencerExpert`)
    - 流程文件: `红人要样或合作处理流程.md`
    - 工具: TSM客服邮箱查询工具

11. **客户索要发票专家** (`InvoiceExpert`)
    - 流程文件: `客户索要发票处理流程.md`
    - 工具: 查询订单基本信息

12. **官网积分专家** (`PointsExpert`)
    - 流程文件: `官网积分处理流程.md`
    - 工具: 查询订单基本信息

13. **官网邮箱信息修改专家** (`EmailModificationExpert`)
    - 流程文件: `官网邮箱信息修改处理流程.md`
    - 工具: TSM客服邮箱查询工具

14. **邮件跟进专家** (`FollowUpExpert`)
    - 流程文件: `邮件跟进处理流程.md`
    - 工具: 查询订单基本信息、查询物流信息

15. **问题已解决专家** (`ResolvedExpert`)
    - 流程文件: `问题已解决回复要求.md`
    - 工具: 无

#### 专家 Agent 标准接口

每个专家 Agent 都遵循统一的接口:

**输入**:
```json
{
  "OldEmail": "邮件原文及历史",
  "BasicInfo": {
    "order_id": "订单号",
    "customer_name": "客户姓名",
    "issue_type": "问题类型",
    ...
  },
  "context": {
    "previous_actions": [],
    "available_tools": []
  }
}
```

**输出**:
```json
{
  "status": "success|needs_info|needs_human",
  "reply_content": "邮件回复内容",
  "tool_calls": [
    {
      "tool_name": "查询订单基本信息",
      "parameters": {"order_id": "xxx"},
      "result": {...}
    }
  ],
  "next_steps": ["等待客户提供地址", "安排仓库发货"],
  "requires_human": false,
  "human_tasks": [],
  "confidence": 0.95
}
```

### 第四层: 工具调用层 (Tool Layer)

统一封装所有 API 接口,提供给专家 Agent 调用。

**工具注册表**:
```python
TOOL_REGISTRY = {
    "查询订单基本信息": {
        "endpoint": "/client/order",
        "method": "POST",
        "required_params": ["order_id"]
    },
    "查询物流信息": {
        "endpoint": "/client/order-tracking",
        "method": "POST",
        "required_params": ["tracking_number"]
    },
    # ... 其他 11 个工具
}
```

## 执行流程

```
1. 用户邮件 → 协调 Agent
   ↓
2. 协调 Agent 提取 BasicInfo
   ↓
3. 调用路由 Agent → 返回 ["物流问题处理流程.md", "商品质量问题处理流程.md"]
   ↓
4. 协调 Agent 依次调用专家:
   4.1 调用物流问题专家
       - 专家调用工具: 查询订单基本信息
       - 专家调用工具: 查询物流信息
       - 专家返回: 回复内容 + 是否需要人工
   4.2 如果需要,调用商品质量问题专家
       - ...
   ↓
5. 协调 Agent 整合所有专家输出
   ↓
6. 生成最终邮件回复
   ↓
7. 如果 requires_human=true,生成人工处理清单
```

## 模型选择策略

根据任务复杂度选择不同模型:

- **协调 Agent**: Claude Opus 4.6 (需要复杂推理和编排)
- **路由 Agent**: Claude Sonnet 4.6 (快速准确的分类)
- **复杂专家** (马克笔质量、商品质量): Claude Opus 4.6
- **简单专家** (问题已解决、发票): Claude Haiku 4.5
- **工具调用**: 无需 LLM,直接 HTTP 调用

## 提示词工程

### 协调 Agent 提示词结构

```
# 角色定义
你是跨境电商客服协调专家,负责整体流程编排

# 核心职责
1. 提取邮件基本信息
2. 调用路由 Agent 获取适用流程
3. 编排专家 Agent 执行
4. 判断人工介入时机
5. 整合输出生成最终回复

# 工作流程
[详细步骤]

# 输出格式
[JSON Schema]
```

### 专家 Agent 提示词结构

```
# 角色定义
你是 [领域] 专家,专注处理 [问题类型]

# 业务流程
[加载对应的标准流程.md 文件内容]

# 可用工具
[列出该专家可用的工具及参数]

# 输出要求
[邮件格式、话术要求]

# 输出格式
[JSON Schema]
```

## 优化策略

1. **缓存机制**: 相同订单号的查询结果缓存 5 分钟
2. **并行调用**: 多个独立工具调用可以并行执行
3. **提前终止**: 如果路由 Agent 返回"问题已解决",直接调用该专家,不再调用其他
4. **置信度阈值**: 专家输出 confidence < 0.7 时,自动转人工
5. **A/B 测试**: 可以为同一专家配置多个提示词版本,进行效果对比

## 监控指标

- **路由准确率**: 路由 Agent 选择的流程是否正确
- **专家成功率**: 每个专家 Agent 的处理成功率
- **人工介入率**: 需要人工处理的比例
- **平均响应时间**: 从接收邮件到生成回复的时间
- **工具调用次数**: 每个请求平均调用多少次 API
- **成本**: 每个请求的 Token 消耗和费用

## 扩展性

1. **新增流程**: 只需添加新的专家 Agent 和流程文件
2. **流程修改**: 只需修改对应的流程文件,不影响其他专家
3. **多语言支持**: 在协调 Agent 层添加翻译逻辑
4. **多品牌支持**: 每个品牌可以有独立的专家配置
5. **人工反馈循环**: 人工修改的回复可以作为训练数据优化提示词
