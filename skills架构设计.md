# Skills 架构设计 — 渐进式技能披露（Progressive Skill Disclosure）

---

## 一、设计背景

### 旧架构的问题

原系统使用独立的 **Router Agent** 做意图识别，匹配到流程后将整个标准流程文件**全量注入** Solver 的 system prompt。

```
load_context → router（LLM调用）→ solver（携带完整流程文件）
```

以物流问题处理流程（14,500 字符）为例，7 轮 ReAct 迭代中这份文件被重复携带 7 次，单次处理消耗约 35,000–50,000 tokens。

### 核心洞察

模型在处理一封邮件时，真正需要的信息只有两层：

1. **意图识别**：这封邮件属于哪类问题？（轻量，约 50 tokens/技能）
2. **执行细节**：这个具体场景下该怎么做？（按需，只加载命中的那一个章节）

其余 14 个技能的执行细节、以及同一技能中未命中的章节，对当前处理完全无用，不应出现在上下文中。

---

## 二、三层架构

```
L1  始终注入（~750 tokens）
    ↓ Solver 识别意图 → 调用 load_skill(skill_id)
L2  场景分支判断表（~200–400 tokens）
    ↓ Solver 对照表识别具体场景 → 调用 load_skill_section(skill_id, section)
L3  单场景执行细节（~500–2,000 tokens）
```

### L1 — 技能注册表（始终可见）

注入到每次 Solver 调用的 system prompt，格式为 Markdown 表格：

```markdown
| ID | 技能名称 | 触发条件 | 说明 |
|---|---|---|---|
| logistics | 物流问题处理 | 物流状态异常、包裹未收到... | 处理运输过程中的物流异常... |
| quality   | 商品质量问题处理 | 产品功能故障、外观缺陷... | 处理非马克笔类产品的质量缺陷... |
| followup  | 邮件跟进处理 | 待跟进状态、跟进邮件... | 会话标记为待跟进时生成跟进邮件 |
| ...       | ...          | ...                      | ...                          |
```

**15 个技能 × 约 50 tokens = ~750 tokens 固定开销**，替代原来 3,500 tokens 的 Router 调用。

### L2 — 场景分支判断表（按需加载）

每个技能文件的 `## L2 场景分支判断表` 章节。Solver 调用 `load_skill(skill_id)` 后获取，用于识别当前邮件属于哪个具体场景，并告知下一步应加载哪个 L3 章节。

格式规范：

```markdown
## L2 场景分支判断表

> 根据邮件历史和客户当前诉求，识别所处场景，调用 load_skill_section(skill_id, <章节>) 加载执行细节。

| 场景 | 判断条件 | 加载章节 |
|------|----------|----------|
| 首次来件，功能性问题 | 客户描述产品无法正常使用，无历史处理记录 | `Step 1. 是否需要提供技术支持` |
| 已提供技术支持，客户反馈仍有问题 | 上一封提供了故障排查步骤，客户回复说仍有问题 | `Step 3. 技术支持后续处理` |
| 在保，需提供解决方案 | in_warranty=是，客户期望换货/退款 | `在保处理流程` |
| 客户同意退款，需执行 | 客户明确同意退款方案 | `执行退款` |
| ...  | ...      | ...      |
```

**判断条件的写法原则**：
- 描述的是**邮件上下文的客观特征**，而非模型的主观判断
- 优先引用**上一封客服回复的内容**（用于跟进场景）
- 优先引用**订单状态字段**（如 `in_warranty`、`Status`、`platform`）
- 避免模糊词如"可能"、"大概"

### L3 — 执行细节（按场景加载）

标准流程文件中的各个章节，包含完整的话术模板、判断逻辑、边界条件。Solver 调用 `load_skill_section(skill_id, section)` 后获取，只加载 L2 表中命中的那一个章节。

---

## 三、工具调用规范

Solver 通过两个工具实现渐进式加载：

### `load_skill(skill_id, level=2)`

```json
{
  "name": "load_skill",
  "params": {
    "skill_id": "followup",
    "level": 2
  }
}
```

返回：
```json
{
  "id": "followup",
  "name": "邮件跟进处理",
  "level": 2,
  "content": "## L2 场景分支判断表\n\n| 场景 | 判断条件 | 加载章节 |\n...",
  "available_l3_sections": ["售后处理类跟进", "退货流程类跟进", "调查处理类跟进"]
}
```

### `load_skill_section(skill_id, section)`

```json
{
  "name": "load_skill_section",
  "params": {
    "skill_id": "followup",
    "section": "售后处理类跟进"
  }
}
```

返回命中章节的完整内容。

---

## 四、Solver 标准执行流程

```
第1轮 ReAct
  思考：邮件是"待跟进"状态，上一封客服回复提到已执行退款
  决策：call_tool → load_skill("followup")
  ↓
第2轮 ReAct
  思考：L2表显示"已执行退款，跟进到账"→ 加载章节"售后处理类跟进"
  决策：call_tool → load_skill_section("followup", "售后处理类跟进")
  ↓
第3轮 ReAct
  思考：已有话术模板，客户名称为 Briana，客服名称为 Winnie
  决策：generate_reply → 生成跟进邮件
```

**最少 3 轮迭代**完成一次完整处理（识别意图 → 加载场景 → 生成回复）。

---

## 五、Token 消耗对比

| 场景 | 旧架构 | 新架构 | 节省 |
|------|--------|--------|------|
| Router 调用 | ~3,500 tokens | 0（已删除） | 100% |
| Solver 首轮（含完整流程） | ~5,000 tokens | ~2,500 tokens（L1+L2） | ~50% |
| Solver 后续轮次 | 重复携带完整流程 | 仅携带已加载内容 | ~60–80% |
| 简单问题（无需 L3） | 全量注入 | 仅 L1+L2 | ~70% |
| 复杂问题（需要 L3） | 全量注入 | L1+L2+单章节 L3 | ~80–85% |

---

## 六、文件结构规范

每个标准流程文件（`标准流程/*.md`）的结构：

```
# 总纲
（核心约束规则，2–5 条，不超过 200 字）

---

## L2 场景分支判断表
（场景→判断条件→加载章节 的 Markdown 表格）

---

# 章节A（L3）
（完整执行细节、话术模板）

---

# 章节B（L3）
（完整执行细节、话术模板）

...
```

**总纲**保留在 L2 之前，作为全局约束规则（如"禁止主动提供换货方案"），Solver 加载 L2 时会同时看到总纲。

---

## 七、注册表配置（`skills/skill_registry.json`）

```json
{
  "skills": [
    {
      "id": "followup",
      "name": "邮件跟进处理",
      "file": "邮件跟进处理流程.md",
      "trigger": "待跟进状态、跟进邮件、确认收货、确认退款到账、催促回复",
      "l1_summary": "会话标记为待跟进时，根据上一封客服回复内容生成对应跟进邮件",
      "l2_section": "L2 场景分支判断表",
      "l3_sections": ["售后处理类跟进", "退货流程类跟进", "调查处理类跟进"]
    }
  ]
}
```

| 字段 | 用途 |
|------|------|
| `id` | 工具调用时的唯一标识 |
| `name` | 显示名称 |
| `file` | 对应的 `.md` 文件名 |
| `trigger` | 注入 L1 表的触发条件描述 |
| `l1_summary` | 注入 L1 表的一句话说明 |
| `l2_section` | L2 章节的 Markdown 标题（用于提取） |
| `l3_sections` | 所有可按需加载的 L3 章节名列表 |

---

## 八、新增或修改技能

### 新增一个标准流程

1. 在 `标准流程/` 目录创建 `xxx处理流程.md`，按文件结构规范编写
2. 在 `skills/skill_registry.json` 中添加一条记录
3. 重启后端（或通过前端"技能注册表"页面操作）

### 修改现有流程

- **修改 L1 触发条件/说明**：编辑 `skill_registry.json` 对应字段，或通过前端"技能注册表"→"编辑元数据"
- **修改 L2 场景分支表**：编辑对应 `.md` 文件的 `## L2 场景分支判断表` 章节，或通过前端"编辑流程文件"
- **修改 L3 执行细节**：编辑对应 `.md` 文件的具体章节，或通过前端"编辑流程文件"
- **新增 L3 章节**：在 `.md` 文件中添加新章节，同时在 `skill_registry.json` 的 `l3_sections` 数组中添加章节名

### 前端管理入口

访问 `http://localhost:8001/web/index.html` → 左侧导航"技能注册表"，可视化查看和编辑所有技能的 L1/L2/L3 内容。

---

## 九、设计权衡

| 权衡点 | 说明 |
|--------|------|
| 最少 3 轮迭代 | 相比旧架构最少 2 轮（Router+Solver），多了 1 轮 load_skill 调用，但整体 token 大幅减少 |
| L2 场景表的准确性 | 判断条件写得越具体，模型命中越准确；过于模糊会导致加载错误章节 |
| policy_content 累积 | 多次 load_skill_section 的内容会累积追加，复杂问题上下文会增长，但仍远少于全量注入 |
| 章节名必须与文件标题一致 | `l3_sections` 中的名称用于正则匹配 `.md` 文件标题，需保持一致 |
